# ロールアウト記録：DigitalCloset

GIGA Standard v5 の `/rollout` を1本通した記録です。
**他のリポジトリにも効く知見**を中心に残します。

- 実施日: 2026-08-05
- 型: **B型**（Vite + React 18 + Tailwind v3 / GitHub Pages）
- 基準点: `76d9be4`
- 作業ブランチ: `claude/rollout-4euqpt`

---

## 結果

| 項目 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満 | 32件 | **0件** |
| タップ領域 44px 未満 | 22件 | **0件** |
| Service Worker | 無し | **登録・更新・圏外まで実測で確認** |
| CSP | 無し | **入れて、止まることを確認** |
| favicon | 296.4 KB | **8.1 KB** |
| 総アセット | 588 KB | 377.4 KB |
| 品質ゲート | 無し | 44項目 + **34通りの壊し方で検査自体を確認** |

詳しい数字は [AUDIT.md](./AUDIT.md) にあります。

---

## 他のリポジトリにも効く知見

### 1. `context.setOffline(true)` では「圏外」を測れない 🆕

**v5 §7-5 の表に `context.setOffline(true)` と書いてあるが、これだけでは足りない。**

Playwright の `setOffline` は**ページ側のネットワークにしか効かない。**
Service Worker は別のネットワーク文脈で動くため、
`fetch(req)` が**本物のサーバーに届いてしまう。**

そのため、

- 「圏外で起動する」は、単にサーバーから取ってきた本体を見て ✅ になる
- `offline.html` のフォールバックは**一度も走らない**（走らないので ❌ になり、そこで初めて気づく）

**確かめ方**：`page.on('response')` で `fromServiceWorker()` を見る。
キャッシュを消したはずのものが 200 で返ってくるなら、サーバーへ行っている。

**直し方**：検査スクリプトが**自分で配信サーバーを起こし、止める**。
`tools/measure-pwa.mjs` がその形にしてある。他のリポジトリへそのまま持っていける。

```js
// 生きている接続も切らないと、止めたつもりで繋がったままになる
for (const s of sockets) s.destroy();
server.close(...)
```

**この形で測り直すまで、PWA の「圏外」の項目はすべて信用できない。**

### 2. 検査は「自分が書いた注意書き」に当たる 🆕

v5 §P4 は `SW_LOCALSTORAGE` がコメントに反応する例を挙げているが、
**同じことが少なくとも2か所で起きた。**

| 検査 | 何に当たったか |
|---|---|
| 拡大禁止（`user-scalable=no`） | `index.html` に残した「`user-scalable=no` は書かない」という注意書き |
| CI が `pull_request` で動くか | `ci.yml` の「`# CI は pull_request でも動かす`」というコメント |

後者は**取りこぼし**なので質が悪い。
引き金を実際に外しても検査が通り抜けた。
つまり「CI が PR で動かないリポジトリ」を、
コメントに単語があるだけで ✅ と判定してしまう。

**横断で効く直し方**

```js
// HTML
src.replace(/<!--[\s\S]*?-->/g, ' ')
// YAML
src.replace(/^\s*#.*$/gm, '').replace(/\s#.*$/gm, '')
// JS/JSX は v5 §P4 の通り（ブロック・行・JSX コメント）
```

そのうえで、**単語を探すのではなく構造を見る。**
`/pull_request/` ではなく「`on:` の下に引き金として書かれているか」。

### 3. `text-gray-400 → 600` の一括置換は必ず濃い面を壊す

v5 §2-8 の警告どおりのことが起きた。**予告どおりなので、最初から測る前提で進めてよい。**

- 白地の 4件を直したら、`bg-gray-900` のカード上の 3件が新たに基準未満（**2.35**）になった
- 同じ行に `bg-` があるかで避けようとしても効かない（面と文字が別の要素）

**手順に組み込むのが正解**：一括置換 → **必ず測り直す** → 濃い面の上だけ逆向きに直す。
「気をつける」では防げない。

### 4. maskable は「中身だけ抜いて」載せると輪郭が消える 🆕

v5 §3-7 は「元の絵から下地だけを取り出して引き伸ばし、ぼかす」としているが、
**逆に「中身だけを抜き出す」ほうが確実だった。**

角丸四角のタイルをそのまま下地に重ねると、
合成したグラデーションとの僅かなずれが**輪郭の影**として残る。
下地そのものを持ち込まなければ、境目は生まれようがない。

**しきい値は測って決める。**

```
下地が3点グラデーションからずれる量  … 最大 42
中身のいちばん近い色（シャツの衿元） … 56
→ 45 で切り分ける
```

はじめ 15 で切ったところ、下地のずれ（最大 42）が「中身」として残り、
**輪郭がうっすら見えたまま「セーフゾーン外 0.064%」という良い数字が出た。**
数字が良くても目で見ると分かる。**画素を数えるのと、実際に見るのは両方要る。**

### 5. `APP_VERSION` は手で上げさせない 🆕

v5 §3-5 は「`APP_VERSION` の更新漏れ」を「更新が反映されない」の原因に挙げ、
「リリース手順書に必ず含める」としている。
**手順書に書いても忘れる。**先読み対象の**実バイトから作れば、忘れようがない。**

```js
const h = createHash('sha256');
for (const p of precacheFiles.sort()) { h.update(relative(DIST, p)); h.update(readFileSync(p)); }
const version = 'v' + h.digest('hex').slice(0, 12);
```

ビルドしたファイル名にハッシュが付く型（Vite / webpack）では、
**先読み一覧も手で書けない**（次のビルドで必ず古くなる）。同じ仕組みで一緒に埋める。

埋め込みに失敗したら**必ずビルドを落とす**こと。
黙って `'dev'` のまま配ると、「更新が反映されない」と「圏外で真っ白」が同時に起きる。

### 6. カードを `<div onClick>` で作ると、キーボードでどこにも行けない

このアプリでは、服の一覧のカードが `<div onClick>` だった。
**Tab で辿り着けないので、キーボードだけでは1着も詳細を開けない。**

`.tap-44` のような当たり判定の工夫は「押しやすさ」の話で、
**そもそも押せるものとして扱われているか**は別の問題。
カード内にボタンが入れ子になっていて `<button>` で包めない場合は、
カード全面に広がる `<button>` を敷いて `z-index` で重ねると両立する。

**横断で数える**：

```bash
grep -rn "<div[^>]*onClick" $(git ls-files '*.jsx' '*.tsx')
```

### 7. `.tap-44` は `@layer components` に入れないと位置指定を奪う 🆕

**v5 §2-9 のコード例をそのまま貼ると、Tailwind のリポジトリでは壊れる。**

```css
/* ❌ そのまま貼ると .absolute より後ろに並び、position を奪う */
.tap-44 { position: relative; }

/* ✅ Tailwind では components 層に入れる */
@layer components {
  .tap-44 { position: relative; }
  /* … */
}
```

このアプリでは、カードの隅の「AIに相談」ボタンと検索欄の「×」が**左下へ落ちた。**

**いちばん怖いのは、数字では気づけないこと。**
コントラストもタップ領域も 0件のまま、見た目だけが壊れる。
`.tap-44` を当てた先に `absolute` / `fixed` / `sticky` がある限り、どのリポジトリでも起きる。

**横断で数える**：

```bash
grep -rn "tap-44[^\"']*\(absolute\|fixed\|sticky\)" $(git ls-files '*.jsx' '*.html')
grep -rn "^\.tap-44 *{" $(git ls-files '*.css')   # @layer の外にあれば要注意
```

**計測にも足した**：`tools/measure.mjs` が
「`.absolute` と書いてあるのに `getComputedStyle().position` が `absolute` でない」
要素を数える。自分で足したクラスが Tailwind の指定を奪う形は全部これで捕まる。
他のリポジトリへそのまま持っていける。

### 8. 測るときは「空の状態」だけでは足りない

一覧が空だと、いちばん面積の大きい画面（一覧・詳細・統計）を測れない。
IndexedDB / localStorage に見本を入れてから、もう一周する。
`tools/measure.mjs` の `SEED` がその形。

一方で、**空状態・エラー文・案内文はいちばん読みにくいことがある**（§2-8）ので、
空の状態も1周は測る。

---

## このリポジトリでやらなかったこと

| やらなかったこと | 理由 |
|---|---|
| `src/App.jsx`（2,199行）の分割 | §P3 の通り自動ではやらない。分割案を AUDIT.md に提示 |
| ESLint の導入 | 分割に合意が出たら同じ PR で入れる（`no-undef` が要るのは分割してから） |
| 提示モード・印刷CSS | 個人が自分の持ち物を管理する道具で、一斉授業にも配布にも使わない |
| 学習ログ `study.v1` | 学習系ではない |
| 一覧の仮想スクロール | 数百着を登録する使い方が実在するか分からない。先回りしない |

---

## フリート横断で数えるべきこと

このリポジトリで見つかった形が、他に何本あるか。

```bash
# 1. カードが div + onClick（キーボードで到達できない）
grep -rln "<div[^>]*onClick" $(git ls-files '*.jsx' '*.tsx')

# 2. 下タブ・補助文字が gray-400 / slate-400（白地で比 2.5 台）
grep -rn "text-gray-400\|text-slate-400" $(git ls-files '*.jsx' '*.html')

# 3. 白抜き文字を載せた bg-*-500（比 3.7 前後で基準未満）
grep -rn "bg-red-500\|bg-emerald-500\|bg-green-500\|bg-blue-500" $(git ls-files '*.jsx' '*.html') \
  | grep "text-white"

# 4. APP_VERSION を手で書いている sw.js
grep -rn "APP_VERSION *= *'v[0-9]" $(git ls-files '*sw.js')

# 5. apple-touch-icon に icon-192 を流用している
grep -rn "apple-touch-icon" $(git ls-files '*.html') | grep "icon-192\|icon-512"

# 6. Service Worker の登録が useEffect の中にある
grep -rn "serviceWorker.register" $(git ls-files '*.jsx' '*.js') -B5 | grep -n "useEffect"
```

**3 と 4 は、このアプリでは実際に両方あった。**
下タブの gray-400 は5画面すべてに出るので、1本あたり20件以上に化ける。

---

## 引き継ぎ：作業環境の制約

- `gigayama.github.io` へは到達できない → **本番の確認はできない。**
  すべて `dist/` をローカルに配って測った
- `generativelanguage.googleapis.com` へは到達できない →
  **Gemini API の往復は未計測。**CSP が止めていないことだけ確認済み
- 実機（Chromebook / iPad / 電子黒板）は無い →
  エミュレーションと画素の計測で代替。**実機での見え方は未計測**
- npm レジストリへは出られる → `sharp` / `playwright` は使える
