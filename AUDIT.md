# GIGA Standard v5 監査：デジタルワードローブ

- 対象: `GIGAyama/DigitalCloset`（**B型** — Vite + React、GitHub Pages）
- 実施日: 2026-08-05
- 監査前の基準点: `76d9be4`（`main`）

**この文書の読み方**

- 「実測」の欄に数字があるものは、**実際にブラウザで動かして測った値**です
- 測っていないものは **「未計測」** と書いてあります。`—`（対象外）とは違います
- 測り方は `tools/measure*.mjs` に入っています。誰でも同じ数字を出せます

---

## 使った道具と測り方

| 道具 | 何を測るか |
|---|---|
| `npm run check` | 読めば分かること（法務・依存・構成・上限値） |
| `npm run verify-gate` | **検査そのものが働いていること**（34通りの壊し方で確認） |
| `npm run measure` | 実ブラウザで全画面を歩き、コントラスト・タップ領域・横スクロール |
| `npm run measure:pwa` | 登録・初回リロード・更新・他アプリのキャッシュ・圏外 |
| `npm run measure:csp` | CSP が実際に効いているか |

コントラストの測り方は、色の文字列から数値を拾うのではなく
**1px 実際に塗って `getImageData` で読み返して**います。
いまは Tailwind v3 なので `rgb()` ですが、v4 に上げると `oklch()` になり、
数値を拾う測り方はそこで全部壊れます。先に壊れない形にしてあります。

測るときは以下も考慮しています。

- グラデーション背景は `backgroundColor` が透明になるので `backgroundImage` も見る
- 絵文字はフォント自身の色で描かれるため除外する（このアプリはモード切替の 💬🛑🔍 が該当）
- `disabled` / `cursor-not-allowed` は WCAG の対象外なので除外する

---

## 実測（前 → 後）

すべて同じ手順・同じ画面（クローゼット／コーデ／カレンダー／ダッシュボード／設定／
アイテム追加／アイテム詳細／廃棄済み／AI相談室）で測っています。
一覧が空だと面積の大きい画面を測れないため、見本データを入れた状態で測りました。

| 項目 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満 | **32件** | **0件** |
| タップ領域 44px 未満 | **22件** | **0件** |
| JS エラー | 0件 | 0件 |
| CSP 違反 | （CSP 無し） | **0件** |
| 読み込み失敗 | 0件 | 0件 |
| Service Worker の登録 | **無し** | **activated** |
| 初回訪問の画面遷移 | 1回 | **1回** |
| 320×568 の横スクロール | 無し | 無し |
| 初回 JS | 250.8 KB | 257.6 KB |
| 総アセット | 588 KB | **377.4 KB** |
| favicon.png | **296.4 KB** | **8.1 KB** |

### いちばん読みにくかったのは「行き先の一覧」だった

下のタブの、いま選ばれていない4つの文字が `text-gray-400`（`#9ca3af`）で、
白地での比は **2.54** でした。5つのうち常に4つがこの状態です。
装飾に見えますが、これは「どこへ行けるか」を示す一覧そのものです。

| 場所 | 文言 | 色 | 前 | 後 |
|---|---|---|---:|---:|
| 下タブ（未選択） | クローゼット／コーデ／カレンダー／ダッシュボード／設定 | `gray-400` → `gray-600` | **2.54** | 7.56 |
| 確認の窓 | 「削除する」など白抜き | `bg-red-500` → `bg-red-700` | **3.76** | 6.47 |
| 通知の帯（エラー） | 「設定からAPIキーを登録してください」 | `bg-red-500` → `bg-red-700` | **3.76** | 6.47 |
| 通知の帯（完了） | 「保存しました」など | `bg-emerald-500` → `bg-emerald-700` | **2.54** | 5.48 |
| 詳細 | 「このアイテムを廃棄する」 | `red-500` on `red-50` → `red-700` | **3.44** | 5.91 |
| 詳細 | 「CPW (1回あたり)」 | `indigo-500` on `indigo-50` → `indigo-700` | **3.99** | 7.07 |
| 廃棄済み | 「削除」 | `red-600` on `red-50` → `red-700` | **4.41** | 5.91 |
| ダッシュボード | 着用ランキングの順位（金・銀・銅） | `yellow-500`→`yellow-700` ほか | **1.92** | 4.92 |
| カレンダー | 曜日の「日」「土」 | `red-500`／`blue-500` → `-700` | 3.76／3.68 | 6.47／6.70 |

取り返しのつかない操作（削除・廃棄）と、
いちばん急いで読んでほしい文（エラーの通知）が、
そろって基準を割っていました。

### 一括置換は濃い面の上の文字を壊した

`text-gray-400 → text-gray-600` を当てたあと、もう一度測ったところ
**新しく4件が基準未満になりました**。

ダッシュボードの「アイテムの資産状況」は `bg-gray-900` の濃いカードで、
その上の薄い文字まで濃くしてしまい、比が **2.35** に落ちていました。

| 文言 | 面 | 一括置換後 | 直したあと |
|---|---|---:|---:|
| 現在のクローゼット | `gray-900`→`gray-800` の帯 | **2.35** | 12.04〜9.96 |
| 廃棄済み総額 | `gray-900`→`gray-800` の帯 | **2.35** | 12.04〜9.96 |
| 累計投資総額 | `gray-900`→`gray-800` の帯 | **2.35** | 12.04〜9.96 |

同じ行に `bg-gray-900` があるかで避けようとしても、
面と文字は別の要素・別の行にあるので効きません。
**一括で直したら必ず測り直す**より確実な方法はありませんでした。

### タップ領域は「ボタンを大きくしない」で直した

22件のうち大半は、詰めて並んでいる帯やツールバーの中のボタンです。
`min-height: 44px` を当てると折り返しが起きて別の破綻を生むので、
`.tap-44::after` で**当たり判定だけ**を広げています。

| 場所 | 前 | 直し方 |
|---|---:|---|
| ヘッダーのAI相談室・追加 | 42×42 | `.tap-44` |
| カテゴリの絞り込み帯 | 82×30 | `.tap-44`（帯の厚みは変えない） |
| カレンダーの月送り | 38×38 | `.tap-44` |
| 廃棄済みの「戻す」「削除」 | 131×32 | `.tap-44` |
| 設定の「編集」 | 48×28 | `.tap-44` |
| ダッシュボードの絞り込み | 66×30 | `<select>` は疑似要素を持てないので `min-h-[44px]` |

`<select>` にこの手は使えません（中身をブラウザが描くため）。
そこだけは高さそのものを確保しています。1つしか置かれていないので押し出しは起きません。

#### `.tap-44` が Tailwind の位置指定を奪った

`.tap-44 { position: relative }` を素で書いたところ、
**Tailwind の `.absolute` より後ろに並ぶため位置指定を奪い、**
カードの隅に置いた「AIに相談」ボタンと、検索欄の「×」が左下へ落ちました。

**この間、コントラストもタップ領域も 0件のままでした。**
大きさも色も正常なので、数字の計測では気づけません。
画面を見て初めて分かりました。

直したうえで、同じ形が二度と黙って通らないよう
**`measure.mjs` に「位置指定が奪われていないか」の計測を足しました。**
`.absolute` / `.fixed` / `.sticky` を持つ要素の `getComputedStyle().position` が
一致しないものを数えます。

わざと直す前に戻して測ると **2件**、直すと **0件** になることを確認しています。

| | コントラスト | タップ | 位置指定 |
|---|---:|---:|---:|
| `@layer` に入れる前 | 0件 | 0件 | **2件** |
| 入れたあと | 0件 | 0件 | **0件** |

直し方は `@layer components` に入れるだけです。
そうすれば utilities の `.absolute` が正しく勝ちます。

---

## PWA の挙動（`npm run measure:pwa`）

**10/10 項目**。すべて実際に動かして数えた結果です。

| 確かめたこと | やり方 | 結果 |
|---|---|---|
| Service Worker が登録されている | `getRegistration()` | ✅ `activated` / scope が `/DigitalCloset/` |
| 初回に勝手にリロードしない | まっさらな状態で1回開き `framenavigated` を数える | ✅ **1回** |
| 押すまで切り替わらない | 版を上げて `update()`、**3秒放置** | ✅ `waiting` のまま |
| 更新の案内が出る | 画面に文言が出るか | ✅「あたらしい版があります」 |
| 押したら切り替わる | ボタンを押して再読み込みを見る | ✅ `waiting` が解け、遷移 +1 |
| 他アプリを巻き添えにしない | 別名のキャッシュを2つ置いてから版を上げる | ✅ **2/2 残った** |
| 自アプリの古い版は消える | 同上 | ✅ 削除済み |
| 圏外で起動する | **サーバーを止めて**読み直す | ✅ 手元の控えから表示 |
| `offline.html` が出る | 本体の控えだけ消してサーバーを止める | ✅ 表示された |

### 圏外の測り方でいちど間違えた

はじめは Playwright の `context.setOffline(true)` で測っていて、
「圏外で起動する」も「`offline.html` が出る」も通っていました。
しかし `offline.html` の側だけ結果がおかしいので調べたところ、
**`setOffline` はページ側のネットワークにしか効かず、
Service Worker は本物のサーバーへ取りに行っていました。**

つまり「圏外で起動した」の中身は、単にサーバーから取ってきた本体でした。
**サーバーそのものを止める**形に直してから測り直しています。
上の表はすべて直したあとの値です。

---

## CSP（`npm run measure:csp`）

**8/8 項目**。入れただけでなく、**わざと止まるはずのものを走らせて確かめて**います。

| 確かめたこと | 結果 |
|---|---|
| CSP が配られている | ✅ `script-src 'self'` を含む |
| `frame-ancestors` を `<meta>` に書いていない | ✅ 書いていない（書いても無視され警告が出るだけ） |
| `script-src` に `'unsafe-inline'` が無い | ✅ `'self'` のみ |
| CSP を入れた状態でアプリが起動する | ✅ 画面が出る / JS エラー 0件 |
| 読み込み時の CSP 違反 | ✅ 0件 |
| **インラインの `<script>` が止まる** | ✅ 実行されなかった |
| **許していない CDN からの script が止まる** | ✅ 読み込まれなかった |
| Gemini API の宛先を CSP が止めていない | ✅ CSP 違反は出ていない |

`style-src` にだけ `'unsafe-inline'` を入れています。
React が `style` 属性で幅などを渡す箇所があり、`style` 属性は
`'unsafe-inline'` でしか許可できないためです。`script-src` は `'self'` のままです。

`connect-src` は `'self'` と `https://generativelanguage.googleapis.com` だけを
列挙しています。このアプリは利用者が宛先 URL を入力する作りではないので、
列挙しきれます（列挙できないものを列挙したふりをして締めると、
動いていた機能が黙って壊れます）。

---

## アイコン（`npm run icons`）

| ファイル | 前 | 後 | 備考 |
|---|---:|---:|---|
| `favicon.png` | 296.4 KB | **8.1 KB** | 16色 / 512→256（`favicon` に 512 は要らない） |
| `icons/icon-192.png` | — | 5.1 KB | 32色 |
| `icons/icon-512.png` | — | 28.3 KB | 32色（上限 60KB） |
| `icons/maskable-192.png` | — | 4.9 KB | セーフゾーン外の中身 **0%** |
| `icons/maskable-512.png` | — | 23.6 KB | セーフゾーン外の中身 **0%** |
| `icons/apple-touch-icon.png` | — | 4.8 KB | **透明なし** |
| 合計 | **296.4 KB** | **74.8 KB** | |

### apple-touch-icon

元の `favicon.png` は角丸四角の外が透明でした。
これをそのまま `apple-touch-icon` に使うと、
**iOS が透明部分を黒で埋めるため、ホーム画面でアイコンの四隅だけが黒く出ます。**
下地で塗りつぶした専用の 180×180 を作っています（透明が無いことを生成時に確かめています）。

### maskable の「余白」

最初は元の絵をそのまま縮めて下地に載せましたが、
**角丸四角の輪郭が薄い影として残りました**（§3-7 が言うとおりです）。

原因は、合成した3点グラデーションが元の絵の下地と僅かにずれることでした。
下地のずれを実際に測ると**最大 42**（256階調のユークリッド距離）ありました。

そこで**元の絵から「中身」だけを抜き出し**、端まで伸ばした下地に載せる形にしました。
下地そのものを持ち込まないので、境目が生まれようがありません。
抜き出すしきい値も当てずっぽうではなく、下地のずれ（最大 42）と
中身のいちばん近い色（シャツの青い衿元で 56）を測ったうえで 45 にしています。

セーフゾーン外の中身は**画素を数えて 0%** です（目標 0.2% 以下）。
下地は切り抜かれてよいので、中身とは色で区別して数えています。

---

## 監査表

### A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama |
| A2 | .gitignore | ✅ | `node_modules/ dist/ .clasp.json .env .DS_Store tools/out/` |
| A3 | dependabot.yml | ✅ | monthly / メジャー更新は自動で上げない |
| A4 | README.md / MANUAL.md / AUDIT.md | ✅ | 3点とも新規作成 |
| A5 | CI が pull_request でも動く | ✅ | `ci.yml`（build → check → verify-gate） |

コミット履歴に `.clasp.json` / `.env` は含まれていません（`git ls-files` で確認）。
`npm audit --omit=dev` は **0 vulnerabilities** です。

### B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（**入れたうえで動作確認済み**） | ✅ | 8/8 項目。インライン・未許可CDNが実際に止まることを確認 |
| B2 | 秘密情報・IDの直書きなし | ✅ | API キーは利用者が入力し `localStorage` にのみ保存 |
| B3 | OAuthスコープ最小 | — | OAuth を使わない（GAS ではない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` は `sw.js` への `SKIP_WAITING` のみ |
| B5 | サーバー側5段ガード | — | サーバーを持たない。個人の端末内で完結 |
| B6 | CDN から取る実行コードが 0 | ✅ | **0 バイト**（Vite が同梱。監査前から達成済み） |
| B7 | 残る外部資産に SRI と版の固定 | — | 外部から読む資産が無いため対象なし |

**B6 は監査前から達成されていました。**
v5 が最優先とする `@babel/standalone` / `cdn.tailwindcss.com` / CDN の React は
1本もありません。P0.5 で行う作業はありませんでした。

### C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | GAS ではない |
| C2 | 自動復旧（シート再生成） | — | 同上 |
| C3 | pagehide で記録確定 | ✅ | 設定値（モデル・カテゴリ・色）を `pagehide` で確定 |
| C4 | 通信失敗時のリトライと明示 | ✅ | AI 相談は3回まで再試行（監査前から）。失敗時は通知に出る |
| C5 | localStorage.clear() を使っていない | ✅ | 0件。自アプリ接頭辞のキーのみ |

服・コーデ・着用記録は操作のたび IndexedDB へ書き込むため、
タブが破棄されても失われません。`pagehide` で足したのは
state にしか無かった設定値です。

### D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | 追加 |
| D2 | 100dvh を使用 | ✅ | `.app-shell` / `@supports not (height: 100dvh)` でフォールバック |
| D3 | safe-area-inset を適用 | ✅ | 左右パディングと下部ナビ |
| D4 | clamp() による fluid type | ✅ | `--fs-body` / `--fs-lead` / `--fs-title` |
| D5 | Canvas に devicePixelRatio 補正 | — | 画面に描く Canvas が無い。`compressImage` は画像を作るだけで、§2-5 の通り配布物は固定値で決めるのが正しい |
| D6 | 320px 幅で横スクロールが出ない | ✅ | 320×568 / 375×667 とも **出ない**（実測） |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` 8箇所すべてに付与。写真は取り込み時に800px/JPEG品質0.7へ縮小 |
| D8 | **コントラスト 4.5:1 以上** | ✅ | **32件 → 0件**（実ブラウザで9画面を走査） |
| D9 | **タップ領域 44px 以上** | ✅ | **22件 → 0件**（疑似要素込みで実測） |
| D10 | prefers-reduced-motion（.01ms） | ✅ | 追加。`0` にすると `fill-mode: both` が壊れて中身が消えるため `.01ms` |
| D11 | forced-colors 対応 | ✅ | ボタン・select・モーダルに枠 |
| D12 | 提示モード | — | 一斉授業で使うアプリではない（個人の持ち物の管理） |
| D13 | 印刷CSS | — | 印刷して配る使い方をしない |
| D14 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` とも無し |

D10 はこのアプリでは特に効きます。全画面が `.animate-in .fade-in`
（`animation-fill-mode: both`）を使っているため、
`0` にすると「動きを止める」つもりが**画面の中身が消える**ことになります。

### E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | 3つとも `/DigitalCloset/` |
| E2 | アイコン4種 + **透明を含まない** apple-touch-icon | ✅ | 5点生成。透明が無いことを生成時に確認 |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | `install-hook.js` を `<head>` 先頭で同期読み込み |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | 合図を受け取れたときだけ。iOS は手順を文章で案内。追加済みなら何も出さない |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除（**実測で確認**） | ✅ | 他アプリのキャッシュ **2/2 残存**を実測 |
| E6 | sw.js が localStorage に触れていない | ✅ | 0件 |
| E7 | 更新通知（押すまで切り替わらないことを実測） | ✅ | 3秒放置で `waiting` のまま |
| E8 | **初回訪問で勝手にリロードしない**（画面遷移1回） | ✅ | **1回** |
| E9 | **Service Worker が実際に登録されている** | ✅ | `activated` |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | `<script>` 0個・外部URL 0個。再読み込みは `<a href>` |
| E11 | APP_VERSION を今回のリリース値に更新した | ✅ | **中身のハッシュから自動生成**。手で上げる余地を無くした |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | **0%**（画素を数えて確認） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | MANUAL.md 2章 |

E11 は「リリース手順書に書いて人間に覚えさせる」形をやめました。
更新漏れは §3-5 が挙げる「更新が反映されない」の最大の原因で、
手順に書いても必ず忘れるためです。`tools/build-sw.mjs` が
先読み対象の実バイトから版を作るので、中身が変われば必ず変わります。

### F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | 通知に `role="alert"`（エラー）/ `role="status"`（それ以外）。装飾アイコンは `aria-hidden` |
| F2 | モーダルに role="dialog"・Esc で閉じる | ✅ | `aria-modal` / フォーカスの閉じ込め / Esc / 閉じたら元へ戻す |
| F3 | キーボードのみで全機能に到達 | ✅ | アイテムのカードを `<div onClick>` から `<button>` へ。**改修前は Tab で詳細を開けなかった** |
| F4 | rt の色を決め打ちしていない | — | ふりがなを使う画面が無い（児童向けではない） |
| F5 | 初回JS 300KB以下 | ✅ | **257.6 KB**（gzip 74.4 KB） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ⚠️ | `src/App.jsx` **2,199行 / 129 KB**。上限内だが後述 |

### G. 学習ログ

| # | 項目 | 判定 |
|---|---|:--:|
| G1 | study.v1 準拠 | — |
| G2 | 中断記録・5分ルール | — |

学習系アプリではないため対象外です。

---

## 品質ゲートそのものの検査

`npm run verify-gate` は、**34通りの壊し方**で複製を壊し、
狙った検査がちゃんと落ちることを確かめます。**34/34 が落ちました。**

「0件でした」だけでは、検査が働いているのか何も見ていないのか区別できません。
実際、この確認をしたことで**検査側の不具合が2件**見つかりました。

| 見つかった不具合 | 中身 |
|---|---|
| `D14_NO_ZOOM_BLOCK` の誤検知 | `index.html` に残した「`user-scalable=no` は書かない」という**注意書き**に反応して落ちた。正しく書いたうえで理由を残すと検査が落ちるのでは、注意書きを消すほうへ力が働く。判定前に HTML コメントを落として直した |
| `A5_CI_ON_PR` の取りこぼし | `/pull_request/` を素朴に探していたため、「# CI は pull_request でも動かす」という**コメント**に当たって通っていた。**引き金を実際に外しても検査が通り抜けた。** コメントを落とし、`on:` の下の引き金として書かれているかを見る形に直した |

どちらも v5 §P4 が挙げている `SW_LOCALSTORAGE` の誤検知と同じ形です。
**検査を書いたら、必ずわざと壊して確かめる**という手順そのものが効いています。

---

## 測っていないもの

**これらは「✅」ではありません。**

| 測っていないもの | なぜ |
|---|---|
| 本番（`gigayama.github.io`）での動作 | 作業環境から到達できない（プロキシ 403）。すべて `dist/` をローカルに配って測った |
| Gemini API が実際に応答すること | 作業環境から `generativelanguage.googleapis.com` へ出られない。**CSP が止めていないこと**は確認済みだが、API キーを使った往復は未計測 |
| 実機の Chromebook / iPad / 電子黒板での見え方 | 実機が無い。1366×768 と 320×568 / 375×667 のエミュレーションで測った |
| iOS のホーム画面でアイコンの四隅が黒くならないこと | 実機が無い。**apple-touch-icon に透明が無いこと**は生成時に画素で確認済み |
| Android のホーム画面で maskable が正しく切り抜かれること | 実機が無い。**セーフゾーン外の中身 0%** は画素で確認済み |
| 実際にインストールした状態（standalone）での表示 | ブラウザ内でしか測っていない |
| 印刷したときの見た目 | 印刷 CSS を入れていないため（対象外と判断） |
| 大量データ（数百着）での表示速度 | 見本5着で測った。仮想スクロールは入れていない |
| `npm audit` の開発依存側 | `--omit=dev` で 0 件。開発依存は配布物に入らないため見ていない |

---

## 人間に決めてほしいこと

### 1. `src/App.jsx` を分割するか

**2,199行 / 129 KB** で、v5 の上限（5,000行 / 400KB）には収まっています。
ただし B型 の目安である「2,000行を超えたら分割」は超えました。

§P3 の通り**自動では分割していません。**分割するなら次を提案します。

```
src/App.jsx            画面の切り替えと共通の state だけ（〜300行）
src/db.js              IndexedDB（20〜57行目あたり）
src/gemini.js          Gemini API の呼び出し5種（60〜325行目あたり）
src/views/ClosetView.jsx  DetailView.jsx  CalendarView.jsx
                       CoordView.jsx  AiStylistView.jsx  StatsView.jsx
                       AddView.jsx  SettingsView.jsx  AIChatView.jsx
src/components/        FadeImage / ConfirmModal / NavButton / ItemForm ほか
```

分割するなら **ESLint の `no-undef` を必ず通してください。**
import 漏れはビルドを通過して実行時に落ちます。
いまは ESLint を入れていないので、分割に合意いただけたら同じ PR で入れます。

**1機能ずつ、別々の PR にするのが安全です。**

### 2. 大量に登録したときの表示

いまは全件を一度に描いています。
数百着を登録した人が出てきたら、一覧の仮想スクロールが要ります。
実際にそういう使い方が起きているか分からないので、先回りしていません。

### 3. `deploy.yml` と `ci.yml` の関係

いまは別々のワークフローで、どちらも `main` への push で動きます
（`deploy.yml` は Pages への配置、`ci.yml` は検査）。
**検査が落ちても配置は走ります。**
配置を検査の後ろに繋ぐこともできますが、
「検査が壊れたときに配れなくなる」ほうが困る場面もあるため、
そのままにしています。繋ぐかどうかは運用の好みです。

---

## 変更していないもの

v5 の絶対安全規則に従い、次には手を触れていません。

- 関数名・API名・`localStorage` のキー・IndexedDB のストア名と項目名
- アプリ名と機能の文言（「エクスポート」「AIで情報を再解析」などはそのまま）
- 依存のメジャー更新（`npm audit fix --force` は使っていない）
- 画面の構成・操作の順番

色は**色相を変えず、面か文字を1〜2段濃くしただけ**です。
変更前後の比はすべて上の表に書いてあります。
